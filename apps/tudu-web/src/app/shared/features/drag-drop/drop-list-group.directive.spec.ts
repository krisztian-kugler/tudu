import { DropListGroupDirective } from './drop-list-group.directive';

describe('DropListGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new DropListGroupDirective();
    expect(directive).toBeTruthy();
  });
});
